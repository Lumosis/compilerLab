#include<stdio.h>
#include<stdlib.h>
//因为tokens中没有'#'所以无法识别\
’.‘也没有录入构成标识符的元素中 所以’.h‘无法识别

void main(){
    int 56a8 = 09;//09应该是十进制数 这里识别成了八进制数0和\
    十进制9 56a8b不应该是标识符 这里识别成了数字56 和标识符a8

    int a = 0x58PSd;
    //PSd不属于十六进制数字的构成元素

    int a = 15a8;
    //a不属于构成十进制数字的元素

    float a = 00ae95;
    //a不属于构成十进制数字的元素
}